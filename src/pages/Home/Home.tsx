import { Title, Form } from "../../components";

const Home = () => {
  return (
    <div className="flex flex-col items-center justify-center">
      <Title title={"I am bored"} />
      <Form />
    </div>
  );
};

export default Home;
